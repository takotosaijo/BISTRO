# AGENTS.md — 项目入口

给任何新开的会话看的第一份文件。目标：**只看仓库就能回答**「这是什么 / 怎么组织 /
怎么跑 / 怎么验证 / 现在做到哪了」。详细方案见 [docs/PLAN.md](docs/PLAN.md)，
当前进度见 [PROGRESS.md](PROGRESS.md)，历史决策见 [DECISIONS.md](DECISIONS.md)。

## 这是什么

对话式角色扮演产品：用户选《水浒传》里的角色聊天（1v1 或群聊），人物关系由用户自定义，
支持文本与语音，每个会话互相隔离。

与「重演剧情」不同：用户把时间线拨到某一回，改变的是**此刻的世界状态**
（谁认识谁、谁在做什么、谁只知道哪些事），而对话永远发生在当下。

已锁定：《水浒传》100 回本，首发 12 个角色，正史模式（用户互动只改关系与记忆，不改原著大事）。

## 怎么跑

前置：Docker 容器 `mypg` 提供数据库（`127.0.0.1:5433`）。

```bash
make setup     # 建虚拟环境 + 装依赖（已做过可跳过）
make db        # 建库 / 建表 / 灌基础数据
make demo      # 造演示数据：张三 × 林冲 × 第十回
make run       # 起服务 http://127.0.0.1:8000
```

接口一览、数据库与容器的来龙去脉见 [README.md](README.md)。

## 怎么验证

`make check` 是唯一的一致状态判定，它跑测试 + 数据冒烟测试。
**没跑过 `make check`，不许把任何功能项标记为 passing。**

```bash
make check     # 一致状态验证（测试 + 种子数据冒烟测试）
make test      # 只跑测试
make status    # 打印 PROGRESS.md 的快照段
```

## 事实来源地图

| 问题 | 去哪里看 |
|---|---|
| 产品要做什么、为什么这么设计 | [docs/PLAN.md](docs/PLAN.md) |
| 现在做到哪了、下一步做什么 | [PROGRESS.md](PROGRESS.md) |
| 为什么当初这么选（避免"优化"掉有意的设计） | [DECISIONS.md](DECISIONS.md) |
| 数据表结构 | [db/schema.sql](db/schema.sql)（唯一事实来源） |
| 改数据库时的硬约束 | [db/CONSTRAINTS.md](db/CONSTRAINTS.md) |
| 接口与启动方式 | [README.md](README.md) |

## 硬约束（违反即视为把项目做坏）

1. `db/schema.sql` 是数据模型的唯一事实来源。时间线与关系的解析必须走视图
   （`v_effective_relationships` / `v_character_states_resolved`），应用层不许再实现一遍。
2. 不为通过测试而放宽断言。每个 passing 功能项必须有已经跑通的验证命令。
3. 关系边是有向的：改 A→B 不影响 B→A。任何"自动同步两边"的实现都是 bug。
4. 作品版本锁定 100 回本，不改 seed 里的版本设定与人物结局。
5. 数据库容器的数据在匿名卷里：**任何情况下不要用 `docker rm -v`**，
   重建时必须显式挂回 README 里记录的卷名。
6. `.env` 不进版本库（已在 .gitignore）。

## 每次会话开始（上班）

1. 读 PROGRESS.md：现状、进行中、已知问题、下一步。
2. 读 DECISIONS.md：不重复讨论已定的事，不"优化"掉有意的设计。
3. `git status` + `git log --oneline -5`：确认工作区与检查点。
4. `make check`：确认仓库处于一致状态，不通过就先修，别在坏状态上继续加东西。
5. 从「下一步」里挑一件事，在功能清单里把它标成 `active`。

## 每次会话结束（下班）

1. 更新 PROGRESS.md：快照（提交号、测试结果）、功能项状态、会话日志（追加一条）。
2. 跑 `make check`，把结果如实写进快照——失败也要写。
3. 没做完的明确写进「进行中」，不要留下"看起来差不多做完了"的模糊状态。
4. 提交：一个逻辑操作整体做完才提交，commit message 说明做了什么与为什么。

## 多会话并发

同时开多个会话时，PROGRESS.md 是唯一的冲突点：

- 「会话日志」只追加自己的条目，不改别人写的。
- 「功能清单」的状态只能由验证结果驱动；改之前先重读那一行。
- 同一时间尽量只让一个会话修改功能清单表格；其余会话在日志里说明意图。
