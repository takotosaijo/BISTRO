# DECISIONS.md — 决策日志

记录"做了什么选择、为什么、否决了什么"。目的不是写设计文档，而是让新会话
**不要"优化"掉有意的设计**——因为 agent 只看得到代码里的"是什么"，
看不到当初的"为什么"。

格式：日期 · 决策 · 原因 · 否决方案 · 约束。

---

## 2026-09-11 · 时间线用「事件锚点」而不是逐回建模

- **决策**：UI 上仍是章回滑块，数据层用 12 个事件锚点，章回号映射到锚点。
- **原因**：水浒有 100 回，大部分章节的世界状态没有变化；逐回建模工作量爆炸且维护不动。
- **否决**：逐回建表（120 条锚点，状态矩阵 120×12，人工校准成本不可承受）。
- **约束**：锚点必须在状态真正变化处切分。新增锚点时要同时补状态行，否则会出现状态断层
  （第 0 期就踩过一次：林冲从第十回一路"卡"在沧州途中直到第七十一回）。

## 2026-09-11 · 关系边有向，且分 canon / user 双源

- **决策**：A→B 与 B→A 是两条独立记录；同一对关系上用户边覆盖原著边，由视图
  `v_effective_relationships` 统一裁定。
- **原因**："A 恨 B 但 B 仍爱 A"是这类产品的核心乐趣，必须能表达不对称；
  覆盖逻辑写一次比散落在各处实现更可靠。
- **否决**：无向边 + 双方共享一条记录（无法表达不对称）；在应用层做覆盖判断（容易各写各的）。
- **约束**：任何"改一边自动同步另一边"的实现都是 bug。关系解析必须查视图。

## 2026-09-11 · 角色状态用填充语义

- **决策**：`anchor_character_states` 只在状态变化时写一行，
  `v_character_states_resolved` 把最近一次状态沿时间线铺开。
- **原因**：否则要维护 角色数 × 锚点数 的完整矩阵（12×12 起步，加角色就爆）。
- **否决**：每个锚点写全量状态行。
- **约束**：副作用是"忘了写新状态"会表现为状态停滞——这正是第 0 期抓到林冲问题的方式，
  所以改动锚点后必须抽查 `v_character_states_resolved`。

## 2026-09-11 · 时间线接口同时接受 anchor_seq 与 chapter_no

- **决策**：`PUT /api/users/{id}/timeline` 两个参数任选其一。
- **原因**：开发中曾把 `seq=10` 误当成第十回（实际 seq 10 是第七十一回）。
  内部序号与前端滑块的章回号是两套编号，不能只暴露一个。
- **否决**：只暴露 `anchor_seq`（前端每次都要自己换算）。
- **约束**：新增时间线相关接口要明确参数的编号体系。

## 2026-09-11 · 后端用 asyncpg + 手写 SQL，不用 ORM

- **决策**：数据访问层 `app/repository.py` 全是手写 SQL，直接查视图。
- **原因**：`db/schema.sql` 要当唯一事实来源；引入 ORM 等于再维护一套模型定义，
  两边很容易漂移。时间线与关系的解析逻辑已经在视图里，ORM 反而绕远。
- **否决**：SQLAlchemy ORM（模型重复定义）、在 Python 层重写关系解析。
- **约束**：改表结构只需改 `schema.sql`；但如果视图语义变了，要同步 `repository.py` 与测试。

## 2026-09-11 · 模型层默认 mock provider

- **决策**：默认 `BISTRO_LLM_PROVIDER=mock`，离线可跑通整条链路；真实模型走 `openai_compat`。
- **原因**：没有 key、没有网络也能开发和验证管线；mock 会把角色当前处境回显到回复里，
  肉眼就能确认时间线生效。
- **否决**：默认接真实模型（没 key 就跑不起来，测试也不稳定）。
- **约束**：mock 只能验证"链路通"，不能验证"像不像林冲"。角色质量必须接真实模型才能验收（阻塞项 B01）。

## 2026-09-11 · 首期只做正史模式

- **决策**：用户互动只改关系与记忆，不改原著大事；野史模式后置。
- **原因**：一旦允许改写剧情，后续锚点的世界状态组合会爆炸。
- **否决**：一开始就做可改写剧情（状态管理复杂度不可控）。
- **约束**：`sessions.is_non_canon` 只用于标记"不太可能的相遇"，不代表剧情被改写。

## 2026-09-15 · 数据库容器改映射到 5433

- **决策**：容器 `mypg` 从 `5432:5432` 重建为 `5433:5432`，挂回原匿名卷。
- **原因**：homebrew 的 PostgreSQL 15 占了 `127.0.0.1:5432`，Docker 的端口转发只能绑通配地址，
  回环访问会连到 homebrew 那个库（超级用户 `xiaobing`），报"role postgres does not exist"。
- **否决**：改用局域网地址绕开（IP 会随网络变化）；停掉 homebrew 服务（那是另一个可能还在用的库）。
- **约束**：容器数据在匿名卷 `37d4086188b086c57a3bd067e3c3074907099f3c4e20bd39f06da7a8351d8e6c` 里，
  重建时必须显式挂回，**永远不要用 `docker rm -v`**。

## 2026-09-15 · 采用 harness 工程管理：AGENTS.md + PROGRESS.md + DECISIONS.md + Makefile

- **决策**：把项目状态、决策、操作命令全部外置到仓库里，按"仓库即唯一事实来源"组织。
- **原因**：需要多会话并行开发，新会话不能靠口头交代恢复上下文；
  仓库外的东西（聊天记录、脑子里的约定）对 agent 等于不存在。
- **否决**：把所有规则堆进一个巨大的 AGENTS.md（第 4 讲：单一巨型指令文件必然失败，按要求分层放）。
- **约束**：功能项状态只能由验证命令驱动；没跑 `make check` 不许标 `passing`。

## 2026-09-15 · 真实模型选 DeepSeek（`deepseek-v4-flash`），走 OpenAI 兼容协议族

- **决策**：`BISTRO_LLM_PROVIDER=deepseek`，`base_url` 默认 `https://api.deepseek.com/v1`，
  模型 `deepseek-v4-flash`；它和 `openai_compat` 共用同一个 provider 类，只是默认地址不同。
- **原因**：DeepSeek 完全兼容 OpenAI 的 `/chat/completions` + SSE，现有 `openai_compat`
  一行不用改就能接；单独加一个 `deepseek` 名字是为了让 `.env` 自解释（不用手填 base_url），
  也让落库的 `model` 字段能区分是谁生成的（`deepseek:deepseek-v4-flash`）。
- **实测**（2026-09-15，key 已写入本地 `.env`，不入库）：
  - `/models` 只返回 `deepseek-flash` 和 `deepseek-v4-pro`；`deepseek-v4-flash` 是被接受的别名，
    响应里的 `model` 会回落成 `deepseek-flash`。别名哪天失效，改用 `deepseek-flash` 即可。
  - 流式响应里 `delta.content` 与 `delta.reasoning_content` 是两个字段，思考过程不能混进角色回复，
    解析处已按此写并钉了测试（`tests/test_provider_config.py`）。
- **否决**：给 DeepSeek 单独写一个 provider 类（协议相同，纯属重复）；沿用「provider 名写错就
  静默退回 mock」的旧行为（那会让「以为接了真实模型」的事故只能靠回复语气察觉，现在改为直接报错）。
- **约束**：换模型/换供应商只改 `.env`；不要在代码里写死模型名。

## 2026-09-15 · 推理模型默认思考，但保留 `BISTRO_LLM_THINKING` 开关

- **决策**：默认 `BISTRO_LLM_THINKING=auto`（模型自己决定是否思考）；需要低延迟时改成 `disabled`。
- **原因**：`deepseek-v4-flash` 是推理模型，会先流式吐一整段 `reasoning_content` 才吐正文。
  角色扮演要「像本人」，思考通常有帮助，所以默认不关；但 F18 要求 TTS 首句 ≤1.5 秒，
  真做语音时这个开关是必须有的旋钮。
- **实测**（同一个用户问题、第十回林冲，打真实服务 `/messages/stream`）：
  `auto` 首字 2.54s / 总 2.68s；`disabled` 首字 1.80s / 总 1.82s。
  单条短回复的差距约 0.7s；长回复思考 token 更多，差距会拉大（API 层实测过一条
  单句回复花掉 135 个 reasoning token）。
- **否决**：默认就关掉思考（牺牲角色质量换延迟，且这是产品决策不该由实现悄悄定）。
- **约束**：没有 `max_tokens` 保护时，思考可能吃掉整段输出预算并吐出空正文
  （API 层实测：`max_tokens=40` 时正文为空、`finish_reason=length`）。
  `chat_once` 会把空回复兜底成「……」，出现这种情况先查思考是否没关。

## 2026-09-15 · 密钥只进 .env，且用 `make secrets` 兜底

- **决策**：API Key 只写在 `.env`；`.env.example` 留空占位；`make check` 顺带跑
  `scripts/check_secrets.sh`（检查 `.env` 仍被忽略、被追踪文件与提交历史里无密钥）。
- **原因**：密钥一旦进了提交，删文件也还在历史里，push 出去就等于泄露；靠人记着
  「别提交」不如让它可验证。
- **否决**：把密钥放进 `settings.py` 或文档示例（会被复制到各处）；只依赖 `.gitignore`
  （防不住 `git add -f` 和手滑写进别的文件）。
- **约束**：密钥泄漏检查失败时 `make check` 直接失败，不许绕过；真的泄漏过要撤销 key 并改写历史。

## 2026-09-15 · 开发服务端口固定为 8002

- **决策**：`Makefile` 里 `PORT ?= 8002`，`make run` 起在 `http://127.0.0.1:8002`；
  需要时 `make run PORT=xxxx` 临时覆盖。文档、脚本里的示例地址一并改成 8002。
- **原因**：8000 被本机另一个项目占用。8000 是 uvicorn 的默认值，不写死就会撞端口
  （现象是 `make run` 报 `[Errno 48] Address already in use`，容易误以为是代码问题）。
- **否决**：改 `app/main.py` 里的端口（端口属于启动方式，不该写进应用代码）；
  让使用者每次自己加 `--port`（迟早有人忘）。
- **约束**：新增文档/脚本里引用本项目的地址一律用 8002；换端口只改 `Makefile` 的 `PORT`。

## 2026-09-15 · 角色卡分层：恒定层进 prompt，全书轨迹进 canon_arc

- **决策**：`characters.identity` / `personality` / `speech_style` / `knowledge_scope` /
  `bottom_lines` / `taboos` / `sample_lines` 是**恒定层**，判据是「这句话在角色第一次登场时
  是否已经成立」。全书轨迹另存 `characters.canon_arc`，**永不进 prompt**（也不在
  `CHARACTER_COLUMNS` 里，应用读不到）。时序身份只从 `anchor_character_states` 取。
- **原因**：产品的核心承诺是「拨到第十回，林冲就只知道第十回之前的事」。角色卡按全书视角写，
  等于把结局端到角色面前，再靠 prompt 求模型别说——那是求，不是保证。旧兜底提示还点名了
  「坐第几把交椅」，等于先把未来告诉模型。
- **否决**：只改 prompt 措辞（治不了数据里的剧透，换个说法照样漏）；把全书轨迹直接删掉
  （作者写锚点状态、F21 评测都要用）；在应用层过滤（`schema.sql` 是唯一事实来源，
  过滤逻辑放应用里迟早各写各的）。
- **约束**：给角色卡加字段前先问「这条信息在首次登场时成立吗」，不成立就写 `canon_arc` 或锚点状态。
  改完跑 `pytest -k character_card_layers`（12 角色 × 12 锚点全扫）。关系边同理：
  `private_note` 会随时间生效，心里话里也不许出现还没发生的事（武松→宋江的「招安」边即为此切段）。

## 2026-09-15 · 开发库的 schema 变更走临时库验证，不轻易 db-reset

- **决策**：这次给 `characters` 加 `canon_arc`，没有 `make db-reset`。做法是新建临时库
  `bistro_scratch` 从零跑 `schema.sql` + `seed.sql`，再对开发库 `ALTER TABLE ... ADD COLUMN`
  并用临时库生成的 UPDATE 覆盖数据，最后逐字段对比两个库确认一致。
- **原因**：`dev_db.sh` 的建表语句没有 `IF NOT EXISTS`，改 schema 只能从零重建；而
  `make db-reset` 会连演示用户、会话、消息一起清掉——当时用户正在 8002 上开着那条会话。
- **否决**：直接 `make db-reset`（省事，但会把别人正在看的东西删掉）；写一版带
  `IF NOT EXISTS` 的 schema（那等于把迁移逻辑塞进 DDL 事实来源里，得不偿失）。
- **约束**：临时库只是验证手段，`schema.sql` + `seed.sql` 始终是唯一事实来源；
  新环境（以及将来接 CI）一律从零构建。手工同步后必须做两库逐字段对比，不能只跑测试。

## 2026-09-15 · 角色评测：先查 prompt，再查回复；探针里不写被禁的词

- **决策**：`make eval` 的判定分三层——① 送给模型的 prompt 必须出现该锚点的处境词（确定性、
  与模型无关）；② 回复的**负面**检查（出戏词、现代词、别人的自称、提前知道未来）；
  ③ `expect_any` 只在「直接问、必须正面回答」的探针上用。探针集写在
  `evals/character_probes.json`，未来事实表与 F13 的分层测试共用 `evals/canon_facts.py`。
- **原因**：第一版 9/13 全是探针太字面——要求武松向陌生人承认杀人、要求高俅向陌生人交代林冲去向、
  把「那畜生」当成没提老虎。**角色有权拒绝回答陌生人**，这是 prompt 里明写的说话规矩；
  判据不能把「答得像本人」判成失败。第二版又踩到「复述用户的话来反驳」被误判成泄漏。
- **否决**：用否认词窗口做启发式（自检时发现「不错，日后招安」会被当成否认，假阴性；宁可硬判据）；
  让模型给自己打分（主观、不可复现、还更贵）；把 `make eval` 塞进 `make check`（要网络与额度）。
- **约束**：① 探针里不写被禁的词，否则模型复述你的话会被误判——这条比任何启发式都可靠；
  ② 主观质量（像不像本人）永远由人看，脚本只挑硬伤；③ 改 prompt 或角色卡后跑一次 `make eval`。

## 2026-09-15 · 前端先做「试验台」：单文件 HTML，由服务直接托管

- **决策**：`app/static/lab.html` 一个文件搞定，`GET /` 直接托管；不引构建工具、不引框架、不做前端路由。
  内容只有四件：章回滑块、我的人设、角色卡列表、1v1 对话 + 看原样 prompt。
- **原因**：核心体验是「拨到某一回，同一个人换了处境」，这必须用眼睛和手才能感受，看 prompt 感受不到；
  但它又不需要一套正式前端。试验台把这份感受先给出来，同时不碰 B03（平台优先级未定）——
  定平台的前提是先有人真的被这件事打中。
- **否决**：一上来就做正式舞台页（卡在 B03，且没验证过体验就投入工量）；引 React/Vite 等构建链
  （为一个试验台引入工具链，等于把「随手可扔」变成「要维护」）；把页面塞进 `main.py` 当字符串
  （HTML/CSS/JS 混在 Python 里没法读也没法改）。
- **约束**：试验台不是产品前端，别在它上面加立绘、群聊调度这些正式功能；如果要加，先解 B03 并另起正式形态。
  页面改动后跑 `pytest tests/test_lab_page.py`（确认它还被端出来、写死的 slug 还在库里）。
  布局这类东西光看代码看不出来，改完要用浏览器量几何（headerTop / scrollHeight）而不是凭感觉。

## 2026-09-16 · 用户与角色的关系由「人设语义」解析产出，不再默认「素不相识」

- **决策**：角色对用户的初始关系不再写死。保存人设时解析 persona 文本（姓名 / 身份 / 背景），
  抽出「与当前角色的关系」（关系类型、方向、强度、生效范围），落成结构化数据；prompt 以结构化结果为准，
  persona 原文作为背景事实一并注入。用户写「东京城里开酒铺的掌柜」→ 无关系（`stranger`），
  聊天后按互动推进；写「林冲的私生女 / 情人 / 邻居」→ 一开始就带着关系进来。
- **原因**：产品承诺「人物关系由用户自定义」（README），但系统用一句固定的「素不相识」把用户设定盖掉了，
  模型只能自己圆场（2026-09-15 试验台实测：用户说「我和爹吵架了」，林冲答「你这般倔，倒像林某当年」，
  而系统提示里写的是「你们如今是「素不相识」的关系」）。
- **否决**：只注入 persona 原文、让模型每次自行理解（不可复现：同样的人设今天理解成邻居、明天成陌生人，
  而且无法用于阶段推进 / 里程碑 / UI 展示）；要求用户全手填结构化表单（门槛高，
  而「私生女」这类语义本来就需要理解）。
- **约束**：① 解析结果要落库、可被用户手改，手改优先；② 同一段人设重复解析应稳定；
  ③「我是他女儿」是**一次声明产出两个方向**（用户→角色：我怎么看他；角色→用户：他知道我是谁），
  这是声明动作的产物，**不是**「改一边自动同步另一边」（硬约束 3 依旧有效，实现里不许出现同步逻辑）；
  ④ 与原著强冲突的声明（如「我是高俅的亲儿子」）要带冲突标记，让角色以「这事不好对外说」处理，而不是忽略。

## 2026-09-16 · 记忆与关系按时间锚点分层：往前携带，往回忘掉未来

- **决策**：一切「我和角色之间发生过的事」以锚点为界——当前锚点及其之前可见，之后的不可见。
  往回滑，角色就不记得未来那些对话；再滑回来，它们又回来了（消息仍带锚点，只是不渲染）。
  关系（阶段 / 好感 / 信任 / 戒备）同样按锚点分层，不例外。
- **原因**：这是「世界状态切片」在产品上的自然延伸——第 10 回的角色既然不知道第 71 回的世界，
  也不该知道第 71 回你跟他说过什么。
- **与既有决策的关系**：**部分覆盖** PLAN §3.1「记忆随身携带」与 §6「好感度跨时间线延续」——
  向前携带不变，新增「不携带未来」。用户 2026-09-16 明确：过去不会忘，未来要忘掉。
- **否决**：只过滤消息、关系仍全局累积（会出现「第 10 回他对我爱答不理、好感却是 80」的鬼状态）。
- **约束**：`messages.anchor_id` 已存在，过滤可直接做；但 `memories` 缺「在哪一章知道」的列、
  `session_summaries` 只有 `up_to_seq`（表达不了按锚点分段），这两处要改表。

## 2026-09-16 · prompt 按锚点维护：存「锚点输入 + 版本」，另存真实快照

- **决策**：prompt 不再只是「每次请求现拼、发完即忘」。按锚点维护：每一章的 prompt 随
  「该章及该章之前」的消息演进；同时要留痕，能回答「他当时看到的是什么」。
  四维度（用户 × 角色 × 锚点 × 会话）的覆盖优先级一次定死。
- **决策（2026-09-16 定稿）**：存**该锚点的动态输入**（该章及之前的消息摘要 + 该锚点解析出的关系快照）
  **+ 卡片版本与模板版本**，另存一份真实发出时的快照（含模型与时间）。不把整段 prompt 文本当事实来源。
- **原因**：角色卡或模板一改（如 F13 那次分层），历史章节存的整段文本就过期了，查问题时看到的是废纸；
  而 PLAN §2.1 本来就要求角色卡版本化、老会话保持当时行为，用「锚点输入 + 版本」才能复现。
- **约束**：无论选哪种，都要能回答两个问题：① 这一章的 prompt 现在长什么样（可复现）；
  ② 某年某月某一句回复，当时送进去的到底是什么（有快照）。

## 2026-09-16 · 人设解析的时机与「一次声明两条边」

- **决策（2026-09-16 定稿）**：① 关系的解析在**保存人设时做一次**，结果落库，允许用户手动修正，
  手改优先；prompt 装配阶段只读结果、不再现场调模型理解。② 「我是他女儿」这类声明，
  **一次写入两条边**（用户→角色 = 我怎么看他；角色→用户 = 他知道我是谁），
  并在实现里写明这是**声明动作的产物，不是自动同步**——硬约束 3 仍有效。
- **原因**：每次装配都让模型现理解，同一段人设今天解析成「邻居」、明天成「陌生人」，不可复现；
  而且拿不到可编辑、可展示、可用来推进阶段的结构化结果。两条边一次写，是因为「父女」本身是双向事实；
  而硬约束 3 禁的是「改一边顺手改另一边」的隐式同步，两者不矛盾，但必须写清楚以免实现时走样。
- **约束**：解析结果要记录来源（哪段 persona 文本）+ 是否与原著冲突；用户手改后
  重新保存人设不得覆盖手改结果，只能提示有冲突。

## 2026-09-16 · 身份（persona）升为一等实体：一个账号可以有多个「我」

- **决策**：`personas` 取代旧表 `user_personas`。一个账号在一个作品里可以有多个身份
  （玉娆 / 苏娘 / 路人张三），**关系、会话、记忆都挂在身份上**；时间线（拨到第几回）留在账号层，
  因为世界时间是同一个。试验台把「以谁的身份进入」和「我在这个故事里是谁」合并成一块「我的角色」。
- **原因**：要让「同一个账号下创建多个角色并切换」成立。旧模型是「一个账号一个人设」
  （`user_personas` 主键 `(user_id, work_id)`），表达不出来，只能靠多建账号糊；
  而换身份 = 换一个人，历史（会话、关系、记忆）本来就不该共用。
- **否决**：一个角色 = 一个账号（不动表，但用户表被人设滥用，将来接账号体系还得再拆一层）。
- **约束**：① `sessions` / `user_character_relations` / `memories` 上的 `(persona_id, user_id)`
  复合外键锁死「身份属于哪个账号」，杜绝记录挂错人；外键声明为 `DEFERRABLE`，
  这样合并身份时能在同一个事务里先搬 personas 再搬引用它的行。② 时间线永远按账号取。
  ③ 新增会话必须给 `persona_id`。
- **迁移**（2026-09-16，开发库，未 db-reset）：建 `personas` 并从 `user_personas` 1:1 平移（286 行）；
  给 `sessions`（362 行）/`user_character_relations`（118 行）/`memories` 补 `persona_id` 并回填；
  把 `admin-daughter` / `admin-lover` / `lab-ui` 三个开发账号的身份并到 `admin` 名下
  （13 个会话、2 条关系、13 条成员、20 条消息改归属，删掉这 3 个账号的时间线行）；
  最后 `DROP TABLE user_personas`。全过程一个事务，失败即回滚。
